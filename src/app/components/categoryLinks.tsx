import { Category } from "@prisma/client";
import React from "react";
import Menu from "./Menu";

const CategoryLinks = ({ category }: { category: Category }) => {
  return <div></div>;
};

export default CategoryLinks;
