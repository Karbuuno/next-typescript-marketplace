/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "nextjs-marketplace.s3.amazonaws.com",
      "nextjs-marketplace.s3.ap-south-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
